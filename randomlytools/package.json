{
  "name": "randomlytools-static-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "node build-static.mjs && node favicon-setup.mjs && node homepage-enhancements.mjs",
    "preview": "vite preview",
    "clean": "rm -rf dist node_modules/.vite"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
